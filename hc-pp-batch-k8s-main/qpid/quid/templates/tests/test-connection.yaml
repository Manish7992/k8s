apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quid.fullname" . }}-test-connection"
  labels:
    {{- include "quid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
